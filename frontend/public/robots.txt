User-agent: *
Allow: /

# The four reference sites (/demo/...) portray invented businesses and are kept
# out of search results with a runtime noindex, not with Disallow: a disallowed
# page is never crawled, so its noindex is never read — which would keep them
# indexed forever if they ever got picked up.

Sitemap: https://aximbra.hu/sitemap.xml
